
# Figma Site Design Intake — Published URL

Source: https://taffy-navy-70222310.figma.site/

## Collected Assets
- Saved HTML: `artifacts/frontend/figma_site.html`
- Full-page screenshot captured by the agent: `artifacts/frontend/figma_site_screenshot.png` (see screenshot in chat)

## Overview
This intake was created from the published Figma site (no source .fig file available). The pages inspected show an authentication flow with a prominent Login screen and top navigation linking to Register, Dashboard, Tasks, Detail, Create, Profile, Settings.

## Login Screen — Visual & Content Observations
- Page heading: "Welcome back" with subtext "Sign in to continue to your workspace".
- Form fields (vertical stack):
  - Label: "Email address" — single-line text input with left icon (mail) and placeholder "you@company.com".
  - Label: "Password" — single-line password input with left icon (lock) and placeholder like "Enter your..." (truncated in screenshot).
  - CTA: primary button (likely labeled "Login" or similar) — rounded pill, strong purple/indigo fill.
  - Secondary actions present in top nav: "Login", "Register".

## UI Patterns and Tokens (approximations)
- Input style: large height, pill-shaped (full rounded), light background (white) with subtle border and inner left icon.
- Fonts: large, bold H1 for welcome; medium weight for labels; muted subtext.
- Spacing: generous vertical rhythm & ample left/right padding inside inputs.
- Colors (approx):
  - Primary / Accent: strong indigo/purple (used for CTA and active nav)
  - Background: subtle patterned pale-lavender on page sides
  - Text: dark navy/charcoal for headings, medium gray for labels and placeholders

## Validation & Behavior (extracted heuristics)
From the UI and common auth patterns, implement these validation rules:
- `email`: required; must be a valid email format; show inline error under the input on blur.
- `password`: required; minimum 8 characters; show inline error on blur. Consider password strength hint if user types.
- Server errors (401/403): surface as a single banner message above the form (do not duplicate). If server returns field-level errors, map them to the corresponding input.
- Network failure handling: distinguish offline (navigator.onLine) vs backend unreachable and display actionable message.

## Accessibility
- Inputs have visible labels (not placeholders only) — good for accessibility. Ensure labels are programmatically associated with inputs (`<label for=...>`).
- Focus styles should be obvious (outline or ring) for keyboard users.

## Implementation Recommendations
- Implement input components matching the pill-shaped style (`rounded-full`, `py-3`, `px-4`) with a left icon slot.
- Tooltip/inline error: use a small rounded chip that appears above or below the input on validation error (single-line messages).
- Primary button: `bg-indigo-600` / `text-white`, `rounded-full`, `px-6`, `py-3`.
- Add client-side validators on `onBlur` and clear per-field errors on `onChange`.
- Axios interceptor: parse server error bodies and map to field or global error messages; fallback to readable messages (e.g., "Backend unreachable", "You are offline").

## Next Steps (pick one)
1. Implement design tokens (Tailwind config updates) and adapt `Register` / `Login` components to match these styles.
2. If you can provide the original Figma file key (or grant token access), re-run the file fetch to extract exact color, spacing and font tokens.
3. I can generate a small UI component library (`FormField`, `Tooltip`, `PrimaryButton`) and replace the current auth pages with pixel-approximate styles.

If you want me to proceed with (1) or (3), tell me which (I will then implement and run tests/build).
# Figma Design Intake

_This file is generated by `ai/hooks/fetch_figma.py` and summarizes frames, images and heuristic validation hints extracted from the Figma document._

File Key: TBD

## Frames

- **FrameName** (`nodeId`) - image: artifacts/frontend/figma_images/FrameName__nodeId.png

## Validation Hints (heuristic)

- None
